node('maven') {

    slackSend channel: 'monolith', color: 'green', message: "started ${env.JOB_NAME} ${env.BUILD_NUMBER} (<${env.BUILD_URL}|Open>)"

    stage ("Get Source code"){
    
        slackSend channel: 'monolith', color: 'blue', message: "started ${env.JOB_NAME} ${env.BUILD_NUMBER} (<${env.BUILD_URL}|Open>)"

    }
    
    }
